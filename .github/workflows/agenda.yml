name: Agenda Deportiva Diaria — Danivia TV

on:
  schedule:
    - cron: "0 */6 * * *"
  workflow_dispatch:

jobs:
  actualizar-agenda:
    runs-on: ubuntu-latest
    name: Actualizar agenda deportiva

    steps:
      - name: Clonar repositorio
        uses: actions/checkout@v4

      - name: Configurar Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Instalar dependencias
        run: |
          pip install cryptography

      - name: Ejecutar bot de agenda
        env:
          FIREBASE_SERVICE_ACCOUNT: ${{ secrets.FIREBASE_SERVICE_ACCOUNT }}
        run: |
          python update_agenda.py

      # ESTO ES LO QUE FALTA: Crea un commit vacío para forzar a GitHub Pages a despertar
      - name: Forzar despliegue de la web
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git commit --allow-empty -m "Forzando actualización de la web tras actualizar Firebase"
          git push

